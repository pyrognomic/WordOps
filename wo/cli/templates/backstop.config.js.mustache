module.exports = {
  id: "{{slug}}",
  viewports: [
    { label: "phone", width: 375, height: 667 },
    { label: "desktop", width: 1366, height: 768 }
  ],
  scenarios: [
  {{#scenarios}}
    {
      label: "{{label}}",
      url: "{{{url}}}",
      misMatchThreshold: {{misMatchThreshold}},
      requireSameDimensions: {{requireSameDimensions}}
    }{{^last}},{{/last}}
  {{/scenarios}}
  ],
  engine: "playwright",
  engineOptions: { args: ["--no-sandbox"] },
  asyncCaptureLimit: 5,
  asyncCompareLimit: 50,
  paths: {
    bitmaps_reference: "{{{paths_root}}}/bitmaps_reference",
    bitmaps_test: "{{{paths_root}}}/bitmaps_test",
    engine_scripts: "{{{paths_root}}}/engine_scripts",
    html_report: "{{{paths_root}}}/html_report",
    ci_report: "{{{paths_root}}}/ci_report"
  },
  debug: false,
  debugWindow: false,
  report: ["CI", "html"]
};
